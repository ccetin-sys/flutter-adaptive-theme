name: flutter_adaptive_theme
description: Adaptive theming engine for Flutter with Material You and platform-aware styling
version: 2.1.0
homepage: https://github.com/ccetin-sys/flutter-adaptive-theme

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.0
  dynamic_color: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
